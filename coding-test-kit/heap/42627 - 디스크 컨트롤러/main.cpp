#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<int>> jobs) {
    int answer = 0;
    return answer;
}

#include <iostream>
#include <cassert>
int main(){
  assert(solution({{0, 3}, {1, 9}, {2, 6}}) == 9);
  return 0;
}