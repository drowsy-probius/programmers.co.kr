#include <string>
#include <vector>

using namespace std;

int solution(int distance, vector<int> rocks, int n) {
  int answer = 0;
  return answer;
}

#include "iostream"
#include "cassert"
int main(void)
{
  assert(solution(25, {2, 14, 11, 21, 17}, 2) == 4);
  return 0;
}